package ui

import (
	"testing"
)

func Test_readtest(t *testing.T) {
	// _, err := LoadTexture("../res/xxx-1.png", "../res/xxx-1.xml")
	// assert.NoError(t, err, "err")
	// buf, err := ioutil.ReadFile("./canglang_0.xml")
	// assert.NoError(t, err, "error")

	// out := &TexturePlist{}

	// err = plist.Unmarshal(buf, &out)

	// assert.NoError(t, err, "xml decode")
	// for k, v := range out.Frames {

	// 	log.Println(k, v.GetColorRect())
	// }
	//t.Logf("%+v", out)
}
